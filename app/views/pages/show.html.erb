<article>
  <div class="content page page--<%= @page.url_name %><%= ' member-page' if @page.member_page? %>">
    <%= render_page_content(@page).html_safe %>
  </div>
</article>
<%= render 'shared/preview_auto_refresh' if params[:preview] == 'true' %>
