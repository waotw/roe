<article class="content collection-page">
  <div class="collection-page-heading">
    <h1><%= sanitize(@page_heading, tags: %w[span], attributes: %w[class]) %></h1>
    <p class="collection-description"><%= @page_description %></p>
  </div>

  <%# Each template owns its full wrapper because the DOM differs:
      compact is a <ul> with <li>s, full has per-item image regions,
      list/links use a <div class="collection-item"> structure. The
      partials live at app/views/collections/_items_{template}.html.erb
      and the safety-allowlisted controller value guarantees the
      partial exists. %>
  <% if @items.any? %>
    <%= render "collections/items_#{@pagination_template}", items: @items %>

    <!-- Pagination -->
    <% if @total_pages > 1 %>
      <div class="pagination">
        <% if @page > 1 %>
          <% if @base_url %>
            <!-- Static site pagination -->
            <% if @page == 2 %>
              <%= link_to '← Previous', "#{@base_url}.html", class: 'pagination-prev' %>
            <% else %>
              <%= link_to '← Previous', "#{@base_url}/page-#{@page - 1}.html", class: 'pagination-prev' %>
            <% end %>
          <% else %>
            <!-- Dynamic site pagination -->
            <%= link_to '← Previous', collection_path(@filters, page: @page - 1), class: 'pagination-prev' %>
          <% end %>
        <% end %>

        <span class="pagination-info">Page <%= @page %> of <%= @total_pages %></span>

        <% if @page < @total_pages %>
          <% if @base_url %>
            <!-- Static site pagination -->
            <%= link_to 'Next →', "#{@base_url}/page-#{@page + 1}.html", class: 'pagination-next' %>
          <% else %>
            <!-- Dynamic site pagination -->
            <%= link_to 'Next →', collection_path(@filters, page: @page + 1), class: 'pagination-next' %>
          <% end %>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <p>No items found.</p>
  <% end %>
</article>
