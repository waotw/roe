<div class="card post-link-medium<%= ' product-link-medium' if kind == 'product-link' %>">
  <% if image.present? %>
    <img src="<%= image %>"
         alt="<%= title %>"
         class="card-image"
         data-sizes="(min-width: 768px) 400px, 100vw">
  <% end %>
  <div class="card-content">
    <% if variant.present? || price.present? %>
      <div class="card-header">
        <h4 class="card-title"><%= title %></h4>
        <% if variant.present? %><span class="product-variant"><%= variant %></span><% end %>
        <% if price.present? %><span class="card-price"><%= price %></span><% end %>
      </div>
    <% else %>
      <h4 class="card-title"><%= title %></h4>
    <% end %>
    <% if show_subtitle && subtitle.present? %>
      <p class="card-subtitle"><%= subtitle %></p>
    <% end %>
    <% if author.present? || date.present? %>
      <p class="card-metadata">
        <% if author.present? %><span class="card-author"><%= author %></span><% end %>
        <% if date.present? && author.present? %> • <% end %>
        <% if date.present? %><span class="card-date"><%= date %></span><% end %>
      </p>
    <% end %>
    <% if show_excerpt && excerpt.present? %>
      <p class="card-excerpt"><%= excerpt %></p>
    <% end %>
    <a href="<%= url %>" class="card-link"><%= link_text %></a>
  </div>
</div>
